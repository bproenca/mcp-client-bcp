# Getting Started

### Project Setup

Configure the environment variable: `OPENAI_API_KEY` with your OpenAI API key.

### Running the Application

In WSL environment, run the following command to start the application:

```bash
./mvnw spring-boot:run
```